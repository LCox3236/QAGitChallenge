on: workflow_dispatch

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
  list-job:
      runs-on: ubuntu-latest
      steps:
        - name: my-step
          run: ls -al
